// In-game patch notes. The title-screen footer is the running version; this
// module is the human-readable release digest the Changelog screen renders. The
// full historical record lives in docs/CHANGELOG.md and is linked from the UI.
export const RELEASE_VERSION = 'v8.2';
export const RELEASE_CODENAME = 'LADDER';
export const REPO_URL = 'https://github.com/mojomast/tokenarena';
export const FULL_CHANGELOG_URL = `${REPO_URL}/blob/main/docs/CHANGELOG.md`;

export const CHANGELOG = [
  {version:'v8.2',codename:'LADDER',date:'2026-09-19',tag:'Per-team intelligence, a server-authoritative ranked ladder, and deeper Operations field work',highlights:[
    'LATTICE snapshots are per-team now. Each peer receives only its own team\'s private intelligence - team contacts and spots, the order feed, the command and role boards and team wallet views - while shared world state still arrives for every actor. Filtering runs at the server seam, spectators get a deliberately neutral view, and offline/local matches are unchanged. The release ships the visibility leak test that proves a team-1 client cannot read team-0 intel, plus the measured snapshot budget before and after.',
    'Ranked arrives as a server-authoritative ladder. Queue ranked or unranked, with rating persisted through the existing progression store, placement games, documented K and team aggregation rules, and post-match deltas applied exactly once even across a reconnect. Clients cannot submit or spoof a rating; unranked play keeps the exact v8.1 behaviour. The online screens show your rating, rank band and the ranked queue state.',
    'Operations field work deepens. Allied bots now buy and use subagent roles, start terminal HACK / DEPLOY / VAULT channels under the same ownership and contest rules humans get, complete PRIME captures and use traversal devices and depots in ordinary co-op rather than only under the validator flag. Personal REQUISITION can buy a loaner Puma through an authored, authoritative spend action with caps and feedback, and the legacy duplicate terminal state is removed in favour of the single UI contract.',
    'Scope: this release changes simulation and server code, so the authoritative game server restarts and connected multiplayer clients briefly disconnect.',
  ]},
  {version:'v8.1',codename:'FOUNDRY',date:'2026-09-19',tag:'Lattice Foundry terrain and bases, vertical grappling, distinct operator field roles, sculpted models and a first-match field guide',highlights:[
    'The `lattice-slice` stand-in is replaced in place by **Lattice Foundry**, a purpose-built 240 x 144 m industrial theatre that preserves the stable map id, the seven-node graph and the five capturable objectives. Every HQ, front gate, siphon, relay and depot owns a physical compound now: West and East Command behind offset blast screens, buttressed West/East Bastion gate courts, a four-entry Foundry Relay ringed by four 17 m chimney solids, sunken North/South Siphon pump courts, and four drive-through depots with collision piers and service sheds.',
    'Height is a real choice, not a decoration. Eight double-ramped roofs, four slag ridges, terrain basins and two freight chicanes give every lane an elevated option while each objective keeps an ordinary walking route for loadouts without a movement verb. The map is real collision geometry: variable terrain height, 104 collision solids and 1,103 navigable graph nodes at 49,280 static triangles across 85 mesh batches. A 24-seat match constructs in 166 ms with 3.89 ms p95 steps in validation, the first objective contest lands at 11.1 s and multiple fronts are contested for 10.8 s of a 30 s smoke.',
    'Foundry is readable from the ground and the air. Fixed daylight, brighter slate/copper materials, thinner fog and thin terrain-following capture rings replace the old opaque capture disk, and the authored node names reach the HUD: West and East Command, West and East Bastion, Foundry Relay, North and South Siphon. Rotational balance is authored by mirroring and pinned by bidirectional traversal, anchor-to-nav attachment, vehicle clearance, symmetry and sightline tests. The shared movement layer also gets a pinned terrain contract: sloped ground can never read as a ceiling and cancel a jump, a body embedded just under the heightfield heals to the surface instead of falling through it, and boost pads still complete their arc to the authored target.',
    'The grappling hook climbs. The reel lift was being cancelled by grounded movement on the same frame, so the hook only ever moved players horizontally; the reel now carries its own vertical velocity, sweeps the real 3D path, clears a standable lip up to 1.8 m above the anchor and detaches on obstruction, release or arrival instead of driving through ceilings or overhangs. Range (14 m), cooldowns and the carrier no-lift rules are unchanged, and integration tests climb a 5 m roof and a 7 m terrace under `Match.step`, including delayed snapshots and correction replay.',
    'Every one of the nine operators and seven harnesses brings a LATTICE field role, dispatched through a shared declarative vocabulary rather than character-id branches. Mistral captures faster on the move, Grok needs Heat, DeepSeek and Kimi spot from overwatch or on the move, Meta braces to repair a cut link, Claude cleanses, ChatGPT and OpenCode move ammunition, Hermes converts personal REQ into team FLUX, Codex repairs a cut link and its terminal, Cline and Gemini open swap/dash capture windows, Roo wards a point, OpenClaw strips hostile progress and Qwen accelerates timed objectives. Bonuses are bounded - capture takes the strongest contribution up to 1.35x, supply conserves ammunition and shared cooldowns stop stacking - and the whole vocabulary stays inert outside the LATTICE modes.',
    'Operators and weapons were rebuilt for identity and detail. Nine distinct helmet, visor and jaw silhouettes gain refined cuirass, limb and glove geometry, and ten weapons get beveled receivers, hollow turned barrels and per-family cooling hardware under a native distance LOD (18 m with hysteresis). A near operator is 11,972-12,820 triangles at an unchanged 63-65 visible draw objects and falls to 6,216-6,964 triangles at distance; muzzle anchors, ADS, reload mechanisms, grip sockets, animation joints and team materials are untouched. A capsule-height regression found in a real browser was fixed and covered by new bounds tests.',
    'New players get a field guide instead of discovering the mode by accident. Both LATTICE modes open a deployment briefing with the objective, first-minute steps, map symbols and live key bindings; the HUD adds a contextual coach that names the next legal objective and its distance, a supply-link diagram, a "your field role" panel and an interaction prompt beside the reticle for RIDE, CUT, REPAIR, terminals and depot vehicles. Quick start fills a real 4v4 practice roster and a 15-minute clock, and the in-game SCAN / GO / ATTACK -> number -> Enter order flow now works in online matches instead of local play only, with Escape to cancel and chat and weapon keys untouched while no order is armed. `docs/LATTICE-FIELD-GUIDE.md` carries the long form.',
    'LATTICE gets its own procedural earcons - secured and lost ground, rides and terminals, wave calls and the HQ siege alarm - plus capture, depot and route-state spark bursts, all reduced-motion aware and captioned. Two latent LATTICE UI bugs were fixed on the way: the playing HUD shadowed its own board derivation, which could drop the front readout, and the mode record was not recognized by the shared mode check, which broke LATTICE result copy.',
    'The balance gates hold on the new map. PvP 4v4 over eight seeds shows 60.2 percent strict contest (gate 35), 89.8 percent fight-at-point (gate 60), 12.1 percent multi-front, 4.92 average live nodes and no single dominant strategy. Foundry is a longer battleground than the flat slice, and the tutorial tier initially measured below its band (6 wins in 20 seeds), so D1\'s body count scale moves from 0.9 to 0.85; the re-validated sample wins 7 of 14 seeds (50 percent, inside the 35-65 percent band) with no Director exploit alarms.',
    'Scope: `core.mjs` changes (the grapple lift and the class support queue) ship in the same deploy as the map and presentation work, so the authoritative game server restarts and connected multiplayer clients briefly disconnect.',
  ]},
  {version:'v8.0',codename:'LATTICE',date:'2026-09-19',tag:'LATTICE STRIKE: a lattice/economy objective mode, OPERATIONS co-op against a Director, and the networking that carries both',highlights:[
    'Two new modes ship together. `cocs` is LATTICE STRIKE, a 4v4/8v8 objective mode, and `cocs-coop` is LATTICE STRIKE: OPERATIONS, 1-8 humans plus bots against the Operations Director. The LATTICE `PLAYER_LIMIT` is 32 while every other mode keeps its previous cap of 8, so the mode is wired for two full teams without changing existing rooms.',
    'The lattice is the objective. A node pays team income only while a supply line still connects it to the owning HQ, and a team can only capture a node adjacent to ground it already holds, so the front is a readable line instead of scattered capture points. Majority dominance, a comeback-policy bot and a five-node slice are the tuning core.',
    'The economy is three currencies. FLUX is team-scoped and buys subagents, fortifications and tools; REQUISITION is personal; COMMENDATIONS are account-level. A duty Chief (or a seated human commander) turns one-button orders - SCAN / GO / ATTACK - into a push, and per-team `intel`, `contacts` and `roleBoard` keep each side informed.',
    'Traversal and logistics are real objects. Ziplines, jump pads, teleporters and launchers move actors along lanes; forward depots capture and spawn loaners; and the interact edge is wired for humans as well as bots, so `E` rides a device at its anchor, cuts or locks it from the 6 m ring, repairs a sabotaged device, captures a depot and starts a terminal HACK / DEPLOY / VAULT channel with on-screen prompts.',
    'OPERATIONS runs against a Director. The Operations Director spends a PRESSURE budget across five non-respawning waves, pushes the weakest node and ends in an HQ siege lose condition; an intermission spend window opens FLUX sinks (FORTIFY / REPAIR / RESUPPLY / REINFORCE), bonus objectives and 25 percent partial rewards. Roles and per-player gates constrain the push and D1-D4 publish the modifier tables without changing tier HP.',
    'Every cocs action crosses the wire through one seam. Inputs enter only as `Match.step` `inputs.cocs`, sorted by `(tick, peerId, cardId)`, the server stays float-authoritative, token buckets rate-limit orders and a `cocs-reject` message reports refusals. `NetClient` predicts optimistically and reconciles, queue/lobby and the command seat are rung-aware, and validation is team-scoped so an off-team order is refused.',
    'The snapshot budget is rate-only: mode-aware `playerLimit`, `effectiveSnapshotHz` at 20 Hz above 32 actors and no payload slimming. Two-client co-op and PvP harnesses are byte-identical, and a measured 24-actor match is about 1.1 Mbps with 32 actors about 1.5 Mbps.',
    'The balance gates hold on the merged tip. OPERATIONS D1 wins 40 percent over ten seeds, and PvP 4v4 shows 48.1 percent strict contest, 88.1 percent fight-at-point and 30 percent trailing-at-half wins with no single dominant strategy.',
    'Scope: this is the first release since v7.0 to change `core.mjs`, `game/protocol.mjs` and `server/` in the same deploy, so the authoritative game server restarts and connected multiplayer clients briefly disconnect.',
  ]},
  {version:'v7.2',codename:'ECHO',date:'2026-09-18',tag:'The Moth audio assets wired into the game: a ritual ambience bed, baked outcome motifs, a per-arena echo map and the void space',highlights:[
    '`MothAudio` is mounted lazily instead of only existing in the library. `app/page.tsx` registers a deferred `SynthAudio.setMothAudioFactory` that builds `MothAudioBank` + `MothAudio` once a real `AudioContext` and the `ambience`/`effects` buses exist (the first user gesture), so nothing fetches or decodes before then and the layer stays inert when no context can be created.',
    'The baked `bed-ritual` clip plays as a low menu/explore/results ambience on the ambience bus at layer gain 0.4 - chosen against the raw clip\'s -12 dBFS peak - while combat deliberately leaves the space to the score and SFX. Scene routing keeps the bed for `menu`/`explore` and nulls it for `combat`.',
    'Victory and defeat now voice the baked `moth-victory`/`moth-defeat` motifs through `MusicEngine.setMotif`. The results arrangement opts into an external take with `leadMotif`, so it prefers the loaded outcome motif and falls back to the built-in COCS line when nothing is loaded; `SynthAudio.setOutcome` selects the take and the victory/defeat sting route reaches it.',
    'The 153-tap baked `arena` echo map re-tunes the shared effects delay/feedback send that gunfire, explosions and thunder already route into (`SynthAudio.setEchoMap`, applied in `_ensureBuses` and beside `setSpace` on `setAudio` and every arena build). Its depth drives the feedback and the tap levels drive wetness, so the tail gains depth without rebuilding the node graph; `audioStatus().echo` reports the selection.',
    'The neon/void theatres (`neon-vertical`, `aether` and `ironfall-megastructure`) now map to the baked `void` IR, so all six baked reverb spaces - open-air, tunnel, hall, cathedral, cavern and void - are reachable through `mothSpaceFor`.',
    'The layer is silent-safe: it starts nothing without an `AudioContext` or before a clip decodes, and it is fully disabled under reduced motion. The settings reduced-motion toggle forwards through `SynthAudio.setMothEnabled`; `MothAudio.setEnabled`/`setReducedMotion` stop every live bed and tear down the owned space graph, and re-enabling only restores playback when the context is usable and reduced motion is clear.',
    '`audioStatus()` now reports `space` (the active IR), `echo` (the echo map), `moth` (the attached layer status) and `samples`, so the live mix can be inspected without a debugger.',
    'Scope: this is a presentation and audio wiring release. No `core.mjs`, `game/protocol.mjs` or `server/` file changed, so the deploy is web-only and connected multiplayer clients are not disconnected.',
  ]},
  {version:'v7.1',codename:'CHORUS',date:'2026-09-18',tag:'A composed soundtrack on a real CC0 sampled orchestra, Moth pass 3, and the Moth audio pipeline',highlights:[
    'The soundtrack is composed instead of looped. `game/music.mjs` gains a per-bar chord-quality table and functional 8-bar progressions in D natural minor for menu, explore, combat and results (i-VI-III-VII, i-VI-iv-v, the borrowed major V and a Picardy I), so pads, brass and choir voice a real triad instead of a static root.',
    'A recurring COCS leitmotif is developed per scene: an augmented menu statement, a diatonic +2 sequence in bars 5-8, an inversion in combat, a staccato diminution as the low-string ostinato, a Picardy-major resolution at the results, and a bell fragment `[1,2,0,0]` at every eight-bar turn. A shared 32-bar form (intro/build/climax/transition/outro) adds fills at bars 4/8/16/24/28/32 and a four-bar bpm ramp.',
    'Phrases accumulate: `setScene`/`setSoundtrack` no longer reset the transport, so music moves through the form instead of restarting. Strings, bass, drone and lead are raised into the samples natural range, combat now plays sampled strings, and `maxVoices` moves 26 -> 44 with a separate sustained-voice budget (four transient slots reserved) so the beat and melody never starve.',
    'A CC0 sampled orchestra ships: 172 samples (9.55 MiB) baked from VSCO 2 CE and VCSL by `scripts/music-bake.mjs` and served same-origin from `/music/*` - strings sustains and spiccato, low brass and staccato, trumpet pad, timpani hits and rolls, bells, tubular bells, gong, cymbals, harp and taiko. `game/sampler.mjs` streams and decodes Ogg (AAC `.m4a` fallback) lazily, selection is seeded (nearest midi, preferred velocity layer, same-pitch round-robin) and folded into `scheduleChecksum`, and any undecoded or failed buffer falls back to the existing oscillator voice, so Node tests and blocked autoplay stay inert.',
    'The music bus is real: `SynthAudio` owns a `musicBus` between the engine and master, so the settings music slider finally attenuates the soundtrack. The master chain gains a limiter (-10 dB, 4:1, +6 dB makeup) and a tanh ceiling, orchestral seating pans, and a retuned reverb send (wet 0.30 with a 250 Hz high-pass and 30 ms pre-delay). The results screen plays its own arrangement through `setOutcome`, so victory and defeat have distinct music.',
    'Moth pass 3 bakes six new blurred effect sequences behind deterministic generators: bloom (explosions), vortex (teleports), contract (capture rings), rise (heals and support pickups), shield (shield breaks, walls and overshields) and snow (weather drift), 18 jobs at three frames each. They are wired through `_mothFx` and fall back to the previous arc-burst/spark-impact cues when a sheet is missing.',
    'Moth pass 3 also lands four baked reverb spaces - open-air (short early reflections), tunnel (chain slapback), hall (medium square diffusion) and cathedral (a long 7 s build) - alongside cavern and void. `mothSpaceFor(arenaId)` picks one per arena (interiors and tunnels override the open-air default) and `SynthAudio.setSpace(name)` swaps the response on `setAudio` and every arena build, defaulting to open-air with a cavern fallback.',
    'The Moth audio pipeline ships end to end in a new `game/moth-audio.mjs`: a lazy `MothAudioBank` (fetch/decode cache, budget eviction, loop windows) and a `MothAudio` layer for beds, spaces and stingers (at most three concurrent beds, fixed scene routing, inert without an `AudioContext`, before decode or under reduced motion). The first bakes are the 5 s `void` IR, the `arena` echo map (153 taps), the `bed-ritual` ambience clip (10.68 s) and the `moth-victory`/`moth-defeat` motifs. The `ir` baker now extracts taps recursively (`extras.taps`), fixing the long-empty `irs.cavern.taps`, and an offline `repair` command rebuilds descriptors from the committed raw results with no API call or credits.',
    'The two audio branches are consolidated: one record per real space (`cavern`, `open-air`, `tunnel`, `hall`, `cathedral`, `void`) and `ir-openair` is deleted as a byte-identical duplicate of `ir-open-air` and must not be re-added; the surviving `ir-tunnel` is the 3.5 s corridor response. `scripts/merge-baked-variants.mjs` unions the pass-3 and audio baked modules offline and the manifest settles at 80 jobs.',
    'Scope: this is a presentation and audio release. No `core.mjs`, `game/protocol.mjs` or `server/` file changed, so the deploy is web-only and connected multiplayer clients are not disconnected.',
  ]},
  {version:'v7.0',codename:'DOCTRINE',date:'2026-09-17',tag:'The full class and harness overhaul, and a balance sweep that ships',highlights:[
    'The class and harness system is data-driven end to end. `kits.mjs` resolves an operator, harness and gear set into one immutable kit, `resolveKit` is gear-aware, deep-frozen and fingerprinted, and the golden ability-parity fixtures pin the simulation behaviour so a data edit cannot silently change the game.',
    'The hidden harness stat multipliers are gone. The seven spec passives are now behavioural through one id-free helper: OpenClaw Grip widens the melee arc, Hermes Express carries sprint posture through a reload, OpenCode Multiplex continues reload progress across a weapon swap, Claude Code Linted pings threats, Codex Green Build shortens reloads, Cline Off-road improves air control and slides, and Roo Flood Fill widens abilities for less direct damage.',
    'All 21 wing riders are live through the shared trigger vocabulary: every rider resolves by trigger and effect type only - no harness or operator id appears in effect logic (a source-scan test enforces it) - and every numeric shaper clamps through the `EFFECT_BOUNDS` table so riders cannot blow the envelope.',
    'Gear is asymmetric. The scope, heavy-barrel and servo builds trade one axis for another (handling power for mobility, offense for handling, mobility for handling), every item declares its power/cost axis and budget, and `resolveGear` enforces the caps once for every caller (damage <=1.15x, speed <=1.10x, handling >=0.90x, pooled health+armour <=+15 points) and returns a frozen result. A dominance suite proves the eight items stay pairwise non-dominated.',
    'The movement verbs from v6.5 gain a real input: `mobility` binds to KeyX, a MOBILITY touch button and the runtime flag, and it is forwarded to the authoritative server as a held state (never as a one-tick edge, which would fake a release and cancel an active grapple). `PROTOCOL_VERSION` moves to 3.',
    'The class identity is visible while you play and while you pick. A new `hud-class.mjs` mirrors the real ability guards, cooldown multipliers and wing-rider bonuses for the ability ring and renders a movement card with the verb, charges or fuel, wind-up and cooldown. The selection screen gains wing/role/signature chips, a tradeoff passive, movement hook and wing rider for the harness, and MODEL/KIT preview tabs.',
    'Wings read at a glance and every strong effect is telegraphed. Strikers get swept-back fins and a leaner torso, vanguards wider pads and a chest plate, tacticians a sensor mast and toolkit; a pooled telegraph player draws ring, disc and directional-arc cues for wind-up start and interrupt, charge, movement start, landing recovery, empty fuel, slam launch and impact, grapple hook and release, rope placement and expiry, and the threat ping, coloured from the wing palette and reduced to a static low-opacity cue under reduced motion.',
    'Team modes gain respawn loadout switching: `Match.setLoadout` queues an operator/harness pair and consumes it on the next spawn (rebuilding stats, movement and verb state while leaving gear intact), the room validates and normalises through `resolveLoadout`, the v3 LOADOUT control message is dispatched by the game server, and the client remembers the seat pair for reconnects and prediction. A respawn overlay reports what killed you and offers the one legal swap.',
    'Kills are attributed: death events and kill-feed entries carry the killer character and harness plus the ability that landed the blow, the feed and banner render wing and ability chips, the scoreboard shows a wing/spec chip per row, captions cover abilities and movement verbs, and the audio gains per-harness activation motifs, movement foley and per-verb announcer lines.',
    'The v6.6 UI/UX audit landed as global chrome clean-up: header action labels become screen-reader-only without wrapping, the demo dock spans the top edge with 44px hit targets, the results summary box model is pinned and its dead legacy CSS removed, remaining sub-44px menu targets are raised, and the pause modal gets a compact quick block with CC and MOTION pills plus a link into the full settings.',
    'Phase 5 ships a real balance gate and a tuned roster. `scripts/balance-sweep.mjs` runs the policy-neutral sweep (the balance gate) and the policy-on sweep (class expression) into `reports/balance-<release>.json`, with one-match replay of any alarm, a truncation guard that refuses to read a budget-clipped sample, an FFA tie-break, and TTK, route and gear-invariant gates.',
    'The P5-2 tuning result: the single-hit cap is now a roster-wide invariant (`min(90, 0.9 x full health)` at every direct-hit site, not just Deep Compute), so a charge-coil Shock Beam can no longer one-shot a full-HP Kimi; the vanguard and tactician spawn pools were raised (Mistral 112, Grok 110, DeepSeek 126 HP and the rest) while all speeds stayed untouched; Qwen Tool Use, Claude Alignment Review and the Hermes, Roo and Claude Code spec uptimes were cut. Policy-neutral operator win-rate spread fell from 18.6 to 11.4 points, the neutral alarm set fell from six to one (Hermes), and every operator clears the 45% Wilson floor - with no god tier, EHP span 1.33 and the gear invariant passing.',
    'Scope: this is the first release since v5.6 to change `core.mjs`, `game/protocol.mjs` and `server/` in the same deploy, so the authoritative game server restarts and connected multiplayer clients briefly disconnect.',
  ]},
  {version:'v6.6',codename:'BIOME',date:'2026-09-17',tag:'Moth skies per biome, new surface and effect albedos',highlights:[
    'Moth skies are wired per theatre instead of one nebula dome: volcanic maps get the baked ashen sky, frost maps get frost, and the neon/void maps (the Quantum Labyrinth included) get void. The swap replaces the addSky gradient on the existing camera-following dome, so stars, the sun disc, haze, halo and the storm/time-of-day tint keep working; every unlisted map keeps its procedural sky. The old nebula bake stays unused on purpose — it decodes to an all-zero equirect that could only reproduce the black dome.',
    'The surface library now covers the kinds the game actually paints: eight new albedos (metal, rough_stucco and corrugated_metal through blur-v1; metal_grating, diamond_plate, carbon_fiber, riveted_armor and industrial_mesh through deep-fryer-v1) and nine new normal maps (grass, hazard_stripes, hex_paneling, holographic_grid, metal, metal_grating, diamond_plate, rough_stucco and corrugated_metal). Default walls, ceilings, barrels, panels and terrain stop falling back to procedural stone where a bake exists.',
    'Two baked effect sequences are live: arc-burst (plasma, three frames) and spark-impact (ember, two frames) drive remote muzzle flashes, bullet and rail impacts, explosions, vehicle kills, respawns, flag and zone captures, teleporter departures and arrivals, contested payloads and lightning strikes. A new pooled, billboarded MothSpritePlayer runs them with deterministic spawns; the effects are WebGL-only and skipped or frozen under reduced motion, and the rift keeps its bespoke looping player.',
    'Landmarks carry the entanglement LUTs: traversal and teleport pads, objective beacons and capture rings, the payload halo, flags, pickups and the menu rings. Volcanic maps use the ember LUT and the rest arcane, while the plain entanglement bake covers flags and pickups.',
    'Surfaces read as material: next-gen props and architecture pick up rock (rocks, ruins, caverns and tunnels — the tunnel shell now generates world-unit UVs), alien_chitin canopies, brushed_metal barrels and goal frames, rough_stucco and metal walls, and ice spikes. Race and soccer presentation shares the view surface helper: a grass pitch with mown stripes, a caution-striped barrier and brushed-metal goals. Race collision blocks stay deliberately untextured.',
    'Shared Moth textures are cached per name and released exactly once instead of decoded into fresh GPU uploads on every call, and every cached Moth texture is tagged so disposal never frees a texture another material still samples.',
    'Budget honesty: the fidelity pass cost 28 emulator credits — 25 submissions, 22 green on the first pass plus 3 re-runs that replaced near-black blur outputs — lifting the baked buckets to 20 textures / 13 normal maps / 4 skies / 3 effects. The four wiring commits cost 0 credits because they reuse the committed bakes; nothing is fetched at runtime and no API key ships.',
    'A pass-3 effect and space batch deepens that coverage: six new blur-core sequences (effect-explosion, effect-teleport, effect-capture-ring, effect-heal, effect-shield and effect-weather-snow, three frames each at 10–14 fps) replace the reused arc-burst/spark-impact cues for blasts and vehicle kills, traversal teleports, flag and zone captures, heals and support pickups, shield breaks/walls/overshields and snow, falling back to the old cue when a sheet is missing. Five baked convolution spaces (open-air, tunnel, hall, cathedral, plus the original cavern) now follow the arena: tunnels and the Quantum Labyrinth get tunnel/cavern, indoor plazas and arenas get cathedral/hall, and outdoor maps keep the drier open-air tail. The batch cost 26 emulator credits for 22 new jobs (18 effect frames and 4 IRs); two first-attempt IRs were rejected for exceeding the 24-qubit Aer budget and re-submitted after shrinking the lattice. Presentation-only, WebGL-only, reduced-motion aware.',
    'Scope: this is a presentation pass. No simulation, protocol or server code changed — the release is web-only — and every unlisted map, surface and effect keeps its procedural art.',
  ]},
  {version:'v6.5',codename:'MOMENTUM',date:'2026-09-17',tag:'Class movement and signature verbs, and a rebuilt attract demo',highlights:[
    'Every operator owns a movement verb, all nine from one data table: Air Dash, Double Jump, Super Jump, Hover Jets, Brace Slam, Safety Glide, Grapple, Blink Step and Deployable Rope. Each is paid for with explicit charge, fuel, impulse, wind-up, cooldown and landing budgets; five spec hooks (economy, chaining, usage, landing-self, landing-control) shape how a harness spends it; and one shared carrier rule disables, weakens or lifts the verb for flag carriers, the VIP and the Juggernaut instead of per-matchup patches. Race and soccer disable verbs outright, and Instagib, Rocket Arena and Full Arsenal run them weakened.',
    'Nine class signature verbs land in the simulation instead of staying data: Effortless reshapes air control, slides and the hop window; Revision skips holster time between two primaries; Heat builds up to +12% fire rate on consecutive hits; Deep Compute charges a bonus-damage shot; Braced regenerates spawn armor and halves crouch knockback; Alignment Review builds an absorb pool; Adaptive shortens the weapon swap and keeps a first-magazine handling bonus; Long Context leaves brief radar trails and extends the range band; Tool Use speeds pickups, objective interactions and vehicles. The ability router now dispatches on ability kind, and NPCs never inherit class kits.',
    'Bots express their class: every kit carries an AI movement policy (engage, escape, hold, route, reposition) layered on the existing archetypes, so bots dash, hover, glide and grapple under the same budgets humans use. The integration suite proves the verbs fire, land and are budgeted in real matches.',
    'Movement and verb state ride the netcode: the actor snapshot carries the documented movement and signature-verb state, a snapshot resync heals the live state object instead of replacing it, and the prediction shadow builds the real player loadout instead of the default pair — a class can no longer desync its own client.',
    'The attract demo is now directed by what is actually happening: an action-directed shot planner scores firefights, kills, contested zones, flag carriers, payload pushes, vehicles, race overtakes and soccer attacks for relevance, visibility, framing, clearance, continuity and repetition, holds each shot through a window with hysteresis, and cuts or blends between them (reduced-motion rules included).',
    'One camera owner per frame: `cameraOwner` (auto/race/manual/free) arbitrates between the director, the race/soccer poses and manual control, planned shots collide with the world again, and manual follow claims and releases through the view API instead of fighting the planner. Free roam seeds from the live pose and hands back to auto with a damped transition.',
    'The Back to Demo view gains a real control dock: Auto Director, Follow Subject, Free Roam with frame-rate-independent flight, boost and adjustable speed, camera-style cycling, reset, HUD toggle, pause and a Demo Options modal with validated draft-vs-applied settings, curated and complete rotation catalogs, mode/map/duration/bot picks and manual pins. Demo settings persist across sessions, and the fullscreen demo keeps its music, ambience, announcer and environment controls.',
    'Two demo bugs are fixed: the title reel rebuilt its scenario on every rendered frame (about 140 rebuilds over five minutes, so no shot ever held), and now advances on the same expiry pacing as the demo view, rebuilding only on an advance/restart edge; and returning the session to auto now resets the camera owner, so a manual style can no longer stick. Both have regression coverage.',
    'This is the second class-overhaul phase. Spec tradeoffs, the 21 riders and asymmetric gear (Phase 3), the `mobility` input binding plus movement HUD/touch presentation, `PROTOCOL_VERSION` 3 with respawn loadout switching (Phase 4), and the TTK bands and balance/tier sweeps (Phase 5) remain future work; specs and gear ride along as inert data for now.',
  ]},
  {version:'v6.4',codename:'SPECTRUM',date:'2026-09-17',tag:'Natural materials, a cinematic soundtrack and soundscape, a restrained HUD, and resolution that adapts to the display',highlights:[
    'Surfaces are rebuilt around one seamless multi-scale height/wear field: albedo, roughness and normal derive from the same data, so cracks, wear and relief agree. Measured albedo/roughness correlation 0.874 (pin > 0.75), tile edge steps 0.74-0.95x (was 3.8-11.3x) and normal relief 0.08-0.23 std (was ~0.007). Weathering is per material (stains, run-off streaks, dust, temperature drift), and the moth macro/fracture enhancer now applies to every natural surface instead of only Moth-baked albedo.',
    'Sky and light read as natural rather than neon: day/dusk/night palettes with a warm dust horizon, a thicker dusk haze band, a faint night airglow and mountain silhouettes that keep their depth; ambience and weather tints are de-neoned, and the CPU renderer follows the day/dusk/night phase.',
    'The soundtrack is composed instead of looped: eight-bar progressions with phrase fills, staged combat layers (kick, snare, hats, accents, counter-line, lead), an outro/enter/idle transition machine for scene and intensity changes, seeded noise risers, stereo panning and reverb sends - all original synthesis.',
    'Gameplay sound is layered: single-token weapon reports (transient, body, thump, sub, tail) with deterministic per-shot variation and distance darkening, surface-aware impacts and ricochets, bounded explosion debris, surface footsteps/landings/jumps/slides, wind and tension beds, and objective cues retuned to the mode root. The cavern reverb impulse now loads (the long-standing 404 is fixed) and retries instead of latching failure.',
    'The in-match HUD is restrained: health and armor share one vitals card, the ability and frag gauges are grouped, the objective bar shows one contextual chip, the kill feed is capped at four, control hints settle after the opening minute, and a REDUCED MOTION chip plus explanatory settings copy make that accessibility mode visible. Reduced-motion semantics are unchanged.',
    'Resolution adapts to the display: a pixel-budget cap (Auto/1080p/1440p/Native) stops 2K/4K screens from asking for 4-9x the tuned pixel count - a 4K viewport at 100% now renders a 1080p-class buffer - and a frame-time governor trades resolution before quality tiers (0.5-1x, hysteretic, long frames counted honestly). Benchmarks keep true native 100%.',
    'Glow defaults are calmer: arena glow trims, the energy emissive preset, objective markers and beacons are toned down, while bloom stays user-controlled through the Glow slider.',
    'The first full-suite gate on this branch fixed 36 pre-existing failures from the phase-1 checkpoint (three stale campaign pins, 30 registry placement sweeps, two next-gen tunnel/facade expectations and a singleplayer driver hang over 600s) plus the review-harness lint error; the suite is green.',
  ]},
  {version:'v6.3',codename:'RESONANCE',date:'2026-09-16',tag:'A real soundtrack, smooth first-person motion and cheaper static worlds',highlights:[
    'The audio engine is actually connected. `ArenaView` now receives `SynthAudio` at startup, so mode themes, the ambience bed, thunder, announcer cues and the victory/defeat sting flow through one object instead of being built and never wired.',
    'A composed procedural soundtrack replaces the single low drone: menu, exploration and combat arrangements share one mode theme and chord progression, with bass, percussion, arpeggio and lead layers, four-bar phrase fills, layered intensity crossfades and gently ducked music under stings. Notes are scheduled with a bounded look-ahead on the AudioContext clock, so timing does not depend on frame rate and a suspended tab resumes without firing a backlog.',
    'Music plays in a quiet menu and quiet gameplay (no rockets or gunfire required). Master mute silences every game-audio branch immediately, Music OFF silences only music, and per-bus Music/Effects/Ambience sliders plus a Preview action live in the normal settings panel. Saved mute/music preferences survive and are migrated without unexpectedly enabling sound.',
    'Combat intensity is fed from the single event-dispatch stage before the effect cursor advances, so nearby shots, explosions and deaths drive the soundtrack again. Announcer cues have one owner with a short cooldown, so one event makes one announcement. `AudioContext.resume()` is handled asynchronously and a blocked context surfaces a recoverable status.',
    'First-person presentation interpolation is completed at the simulation boundary: the host captures a presentation snapshot around every fixed step (including catch-up), and the previous/current ticks blend by the accumulator fraction for the local camera, actors, vehicles and projectiles. Mouse-look stays immediate, history resets on match/map/respawn/teleport/seek, pause freezes without a backward jump, and multiplayer keeps its own interpolation.',
    'Static worlds cost less at unchanged 100% scale: floor tiles merge into one material batch on WebGL and visible blocks batch by material within small spatial chunks, while the CPU renderer keeps its separate tiles for depth ordering. Collision geometry, semantic block ids, UVs, normals and shadow behavior are preserved.',
    'Scopes are corrected and polished: the reticle cross now genuinely passes through the aiming centre (the quadratic control point was offset), the centre dot is a bounded CSS-pixel circle instead of a stretched SVG ellipse, the reticle reads the live weapon/optic during an ADS swap, and magnification uses the perspective formula `2*atan(tan(baseFov/2)/magnification)`. Ocular shading and lens warp are removed under reduced motion.',
    'Terrain gets crease-aware smoothed normals and coherent per-vertex tint: continuous slopes read smooth while cliffs and material seams keep hard edges, and authored height/collision are untouched.',
    'Performance tooling is trustworthy: the GPU query now covers the first-person weapon pass, CPU submission is split into world/post and weapon, `tokenArenaPerf` propagates GPU timing into the tracker, outstanding timer queries are bounded and cleared on disjoint events, and `tokenArenaBenchmark.run()` applies the fixed preset scenario for direct and post-processed variants, warms up, measures a window and restores state.',
    'Shader warmup is connected to scene preparation: the selected arena and starting weapon are prepared before gameplay settles, post variants are created, the compile is bounded and token-guarded across rapid map changes, and a lightweight "preparing" state is shown while it completes. Failures are reported rather than treated as success.',
  ]},
  {version:'v6.2',codename:'CADENCE',date:'2026-09-16',tag:'Native-resolution performance, smooth presentation and sighted optics',highlights:[
    'Scopes and ironsights are physically mounted instead of floating: integrated and attached optics get a receiver base plate, paired support posts and clamp rings that sit below or outside the bore, so a raised optic reads as bolted on while the sight line stays open.',
    'Integrated Rail Lance and Marksman scopes now actually zoom: one active-sight resolver combines the weapon\'s built-in sight with any mounted optic and the aiming state, and drives both the ADS field of view and the reticle. The rail lance zooms harder than the marksman rifle, and an attached scope is a middle ground; iron sights keep their mild, floored ADS pull-in.',
    'Scope reticles get a lens treatment: a full-bleed reticle with a barrel-warp bow, mil ticks, a centre dot and a soft ocular shadow, drawn as native-resolution SVG with non-scaling strokes so it stays hairline-crisp at any viewport. Irons and holos keep a small configurable dot.',
    'Aiming is immediate without re-rendering the HUD: the reticle layer owns the hip crosshair and ADS reticle and toggles between them in its own animation frame, so the switch does not wait for the 80 ms HUD snapshot.',
    'ADS recoil no longer doubles through the transition. The weapon pose now blends a neutral hip orientation with the solved ADS orientation first and then composes distinct movement-sway, recoil, reload and weapon-switch channels exactly once, instead of baking recoil into the hip pose and re-adding it after the blend.',
    'Near-wall shots read correctly: the visual tracer origin is clamped to just before a close authoritative impact (and degenerate traces are suppressed), while the impact flash remains and the muzzle flash stays on the real barrel. Authoritative endpoints, hit registration and remote muzzle origins are untouched.',
    'Draw calls drop where they cost most: third-person operators and pickups use a simplified, light weapon silhouette (a handful of meshes instead of roughly forty) that still exposes its type and barrel-tip anchor for remote tracers, while the fully detailed weapon is reserved for the first-person viewmodel.',
    'Shadows skip what is not worth casting: tiny tagged greebles, transparent effects (energy shields, muzzle flashes, visor glow) and the team base ring no longer cast, keeping the shadow silhouette while trimming the shadow draw.',
    'Presentation interpolation (opt-in for the local fixed-step path) retains the previous and current actor transforms and blends by the fixed-step accumulator fraction, interpolating yaw the short way around and snapping on respawn or teleport. 60, 120 and 144 Hz presentation of one 60 Hz simulation stays inside the same tick bracket and never touches simulation state.',
    'Honest baseline tooling: the renderer reports its backend, GPU vendor/renderer and texture limit, the frame cost is split into scene preparation and render submission, and GPU time is only ever filled from a real asynchronous WebGL2 disjoint timer query — never relabelled from CPU submission time.',
    'Loading is less stuttery: shader variants are warmed with compileAsync when available (synchronously otherwise, never on the CPU renderer), cached viewmodels are reused across swaps with a bounded cache, and repeated swaps keep resource counts stable.',
    'Compatibility: the per-frame renderer counter reset is optional-chained and the CPU SoftwareRenderer now exposes a WebGL-compatible info.reset(), so a software-only environment no longer throws every frame and tears down the render loop.',
    'Sight verification is stronger: bore clearance is re-tested with a real camera and Raycaster.setFromCamera() across FOVs and aspect ratios, using an angle-defined clear cone instead of a fixed pixel region, checking every opaque mesh (including scope walls) and confirming the ADS camera stays outside solid receiver and stock geometry.',
  ]},
  {version:'v6.1',codename:'SIGHTLINE',date:'2026-09-16',tag:'Clear sight pictures and correctly rigged weapons',highlights:[
    'Every weapon\'s iron sights and scopes are now rigged onto a clear sight line that sits above the model\'s own top profile. Opening the apertures exposed receiver rails, conduit rods, dorsal coolant tanks and rear-sight bases that used to hide behind solid sight blocks — they no longer block the bore, and a per-weapon raycast test proves the centre of the aiming picture is unobstructed.',
    'ADS framing is fixed: the solver holds the weapon body at a consistent distance in front of the eye instead of pinning eye relief, so weapons whose rear sight sits forward of the model origin (the Scattergun and others) no longer shove the camera inside the receiver and fill the screen with the stock.',
    'A dedicated ADS reticle replaces the hip crosshair while aiming — one small, sharp, high-contrast centre reference (a dot for irons and holos, a thin cross for scopes) that sits at native UI resolution and never competes with the sight picture.',
    'Shots no longer appear to come from the side of the gun: local tracers now originate on the camera\'s aim ray at the muzzle depth, while authoritative hit endpoints and camera aim are untouched. Other players\' shots still leave their own muzzles.',
    'The Pulse Rifle gets a visible charging handle, and the solved ADS pose now carries the recoil pitch and roll on top of it, so firing still kicks the sight picture instead of freezing it level.',
    'The long Rail Lance and Marksman scopes are open, see-through tubes with their own reticle framing.',
  ]},
  {version:'v6.0',codename:'CLARITY',date:'2026-09-16',tag:'Open sights, honest performance and real quality tiers',highlights:[
    'Sights are actually usable. Solid blocks and capped cylinders are gone: iron sights are an open rear notch with a front post, holographic sights are a thin frame around an empty window with a small reticle, and scopes are open-ended tubes with no caps or lens disks across the bore. The target is visible through the sight, not behind it.',
    'Aiming down sights is solved from the weapon\'s real aperture and front-tip anchors after its final scale and attachment transforms, so the sight line lands on the weapon camera\'s centre ray; the SMG\'s rear anchor now matches its actual aperture. Reduced motion snaps to the correct ADS pose instead of falling back to the hip.',
    'Auto quality is automatic again: the saved "auto" setting is no longer stored as a pinned tier, the frame-rate governor needs sustained pressure with a cooldown so it cannot oscillate, and fixed low/medium/high tiers stay fixed.',
    'Quality changes now reduce real work: a zero-strength bloom pass is omitted entirely, bloom extraction has its own capped resolution budget independent of the world, FXAA and vignette are tier-gated passes, the shadow target is resized correctly on tier change, dynamic shadows refresh on an elapsed-time 20–30 Hz budget, and WebGL gets real geometry LOD.',
    'Performance accounting is trustworthy: three\'s per-render counter reset is disabled and the counters are reset once per presented frame, so the totals cover the world, post-processing and the first-person weapon pass. The debug snapshot reports viewport, drawing buffer, active tier, enabled passes, draw calls, triangles, geometry/texture counts, CPU phase timings and frame-time median/p95, with an optional asynchronous GPU number never confused for CPU submission time.',
    'A fixed benchmark preset (same map, seed, roster, weather and camera path) captures a direct and a post-processed run and prints a copyable report. The world render resolution is unchanged throughout.',
    'Presentation pacing and polish: assembled viewmodels are cached and reused across switches, a recorder keyframe is checked before building an expensive snapshot, and separate controls for effects quality, camera shake and weapon bob sit alongside the global reduced-motion option.',
    'Long simulated matches that take minutes without 3D hardware are now opt-in (COCS_SLOW_TESTS=1 / npm run test:game:slow) so the default suite always finishes.',
  ]},
  {version:'v5.6',codename:'ARSENAL',date:'2026-09-16',tag:'Weapon presentation, combat AI and rendering',highlights:[
    'First-person weapons are rebuilt around per-weapon anchors: each gun exposes its own muzzle, front/rear sights, grips, magazine, bolt and hinge, sights come from the weapon itself rather than one shared rail, and aiming down sights resolves to that weapon\'s own sight line and optic.',
    'Weapons have moving parts again: the SMG magazine, Scattergun break-action, Rocket Launcher loading and Rail Lance energy cell animate from real reload progress, the bolt cycles on each shot, and weapon swaps now hold the outgoing gun through the lowering phase and raise the new one — all from authoritative simulation state.',
    'The viewmodel renders through its own scene and camera, so the gun keeps correct depth between its own parts without being clipped by world geometry, with an independent field of view and the existing CPU fallback.',
    'Movement no longer dominates accuracy: precision-weapon movement penalties are far smaller, spread is applied in the plane perpendicular to aim, and the crosshair reads the same effective-spread calculation used by shooting.',
    'Bots handle weapons like players — one shared switch operation with the delay, reload cancel and event, plus selection hysteresis — and navigate with weighted A* routing, reachable cover scoring, laterally separated flanks and cached routes. Spawns now weigh threats, exposure, incoming projectiles and a decaying death heatmap instead of raw distance.',
    'Rendering detail: the composer gets explicit antialiasing and is no longer disabled by zero bloom, reduced motion keeps static material detail, and albedo, roughness and normal maps share one height field so relief and roughness match.',
  ]},
  {version:'v5.5',codename:'FIDELITY',date:'2026-09-16',tag:'Surface, particle and model fidelity',highlights:[
    'Eight new relief surfaces (diamond plate, riveted armour, circuit board, brushed metal, corrugated metal, alien chitin, rough stucco, industrial mesh) with a canonical-kind resolver, intuitive aliases and seam-free tiling, mapped across arena floors and blocks.',
    'Texture generation can now emit a dedicated bump map alongside the normal map, and materials wire it when present.',
    'Particles are richer and cheaper: the effect pool recycles with a single linear scan and reuses scratch vectors/colours, and gains fade curves, damping, custom gravity, spin and colour interpolation — used by rocket exhaust trails, shield-break debris and rail impacts.',
    'Model detail continues: muzzle anchors for the marksman rifle and SMG, cached pickup geometry, and extra Hornet pods/beacons, Puma tail-lights and exhaust pipes.',
  ]},
  {version:'v5.4',codename:'DETAIL',date:'2026-09-16',tag:'Character motion and surface detail',highlights:[
    'Recoil actually kicks now: the weapon rig was writing its impulse to a field the springs never read. It also gains a stride bob tied to the run cadence and a tuck-down, direction-aware weapon swap.',
    'Characters absorb landings with knee flexion and a torso dip, lower the offhand through a reload, flex through strafes and stabilise the head; the first-person viewmodel dips and rolls through reloads and weapon swaps.',
    'Arenas and kits gained detail: new procedural floor and block surfaces (holographic grid, metal grating, carbon fibre, weathered concrete, hex panelling, hazard stripes), a weapon ejection deflector, Hornet fins and skids, a Puma front splitter with hood vents, and extra arm and leg armour plates.',
  ]},
  {version:'v5.3',codename:'IMPACT',date:'2026-09-16',tag:'Weapon, shield and impact feedback',highlights:[
    'Shouldering a weapon now reads: the first-person viewmodel glides onto the iron-sight line while aiming down sights and eases back on hipfire, and holds its fixed hip layout under reduced motion.',
    'Energy shields are visible again: Overshield glows cyan and the Juggernaut carries an amber bubble; shattering one fires a burst of shards, a distinct crack and a shield-break damage event.',
    'Hits read harder: critical and headshot hits get a gold hitmarker and damage number, low health pulses the health card and adds a heartbeat, and killstreak sprees announce themselves with their own cue.',
    'Boosting vehicles spit nitro exhaust and their engine pitch climbs, and campaign transmissions now carry each speaker\'s callsign, tag and colour.',
  ]},
  {version:'v5.2',codename:'RESTORE',date:'2026-09-16',tag:'Campaign persistence and mode fixes',highlights:[
    'Campaign progress survives a reload again: the save normalizer was dropping every completed mission because it required a flag the recorder never wrote, so the campaign silently reset to mission one.',
    'Losses now count as attempts without unlocking the next mission, so mission-select shows real attempt and win counts.',
    'VIP Escort credits objective time and the extraction to the escorting players, so its scoreboard columns and ranking no longer read zero.',
    'Race mode no longer crashes on a circuit with fewer than eight grid slots and can no longer produce a NaN progress on a one-gate track.',
    'Deep horde waves honour the declared live enemy cap, an elite wave is only announced when an elite actually spawns, a mounted gunner or passenger sees their PUMA card, and onboarding now traps keyboard focus.',
  ]},
  {version:'v5.1',codename:'TIGHTEN',date:'2026-09-16',tag:'Progression and UI correctness',highlights:[
    'Killstreak challenges are now a high-water mark: a weekly "reach an 8 killstreak" objective can no longer be completed by several smaller streaks across matches.',
    'The scoreboard no longer shows a healthy 0 ms ping when no latency was reported, and an unassigned team is no longer grouped under RED.',
    'The solo objective counter stops at the total instead of overrunning it when a mission keeps running after the last step.',
    'The Graphics & settings dialog now traps keyboard focus like the other modals, and a stale duplicate star-rating rule was removed.',
  ]},
  {version:'v5.0',codename:'COMPACT',date:'2026-09-16',tag:'Id-keyed snapshot deltas',highlights:[
    'Snapshots are now sent as compact patches: the actor, rocket and pickup arrays are diffed element by element against the previous frame instead of crossing the wire whole. Measured frames drop about 90% in size (roughly 30 KB down to 3 KB per frame in a full 8v8 match).',
    'The protocol only sends a delta to a client that advertised support, so an older tab keeps receiving full snapshots; a periodic full keyframe lets a client that missed a frame re-sync within a second.',
    'The server exposes delta/full frame counts in its status JSON and the debug hook reports delta hits, misses and bandwidth, so the savings are measurable in a live match.',
  ]},
  {version:'v4.17',codename:'STREAMLINE',date:'2026-09-16',tag:'Netcode consistency and cleanup',highlights:[
    'The live render path now shares the exported snapshot interpolator, so the renderer and the deterministic net harness can no longer drift apart on actor, rocket or vehicle blending.',
    'A single control message larger than the transport budget no longer blocks every later reply: congested sockets drain essential replies through a pure scheduler that keeps later messages moving.',
    'Removed a set of dead exports across the race camera, vehicles, showcase, models, arenas, presets, progression and bot code, and documented why snapshot deltas are still server-side future work.',
  ]},
  {version:'v4.16',codename:'SIGNAL',date:'2026-09-16',tag:'Demo continuity and award data',highlights:[
    'The title demo no longer drops to the operator preview after the first scenario when Reduce motion (or a saved display setting) is on; saved display settings are applied before the first demo builds and the attract reel keeps cycling.',
    'The end-of-match BEST ACCURACY and MOST DAMAGE awards now fire: the simulation tracks per-actor shots, hits and damage alongside the objective stats.',
    'Matchmaking keeps a queued player\'s career identity when the draft seats them, and the public queue list never exposes the profile owner token.',
    'Every lobby/matchmaking wire verb is declared in the shared MESSAGE list, guarded by a drift test so the client and server switches cannot diverge.',
  ]},
  {version:'v4.15',codename:'AMBIENT',date:'2026-09-16',tag:'Demo audio and environment options',highlights:[
    'The fullscreen demo controls gain a music toggle (independent of the global mute), ambience and announcer toggles, and an environment (weather) picker: AUTO plus clear, overcast, rain, snow, ash and storm.',
    'The pinned weather now stays put for the whole cinematic demo instead of being reset every frame; starting a real match still restores automatic weather.',
    'Music, ambience, announcer and environment choices persist across sessions and survive unrelated settings changes.',
  ]},
  {version:'v4.14',codename:'TUNED',date:'2026-09-16',tag:'Objective-mode and lobby correctness',highlights:[
    'Holdout, Uplink and VIP Escort now show the correct command brief, scoreboard columns, ranking and match-start target instead of generic frag or hill copy.',
    'Juggernaut and Team Elimination raise the SUDDEN DEATH banner that their self-managed objective state sets.',
    'Bots treat Holdout, Uplink and VIP Escort as objective modes and resupply toward the objective rather than wandering the map.',
    'Campaign announce beats are surfaced as HUD notices, and the server no longer lets a disconnected or reconnected peer keep a map or rematch vote; a reconnect must ready up again.',
    'Menu polish: Match Setup returns focus to its trigger, the Theater camera hotkeys cover all eight rigs, and the title overlay no longer blocks the footer GitHub link.',
  ]},
  {version:'v4.13',codename:'ATTRACT',date:'2026-09-16',tag:'Title-screen demo controls',highlights:[
    'The demo announcement bar now belongs to the title screen foreground, so it no longer floats over the main-menu controls.',
    'The menu top bar gains a fullscreen toggle that mirrors the browser fullscreen state.',
    'A "Back to demo" button returns to the live demo, hides the title overlay, and offers on-screen and arrow-key controls to cycle through the demo modes.',
  ]},
  {version:'v4.12',codename:'SYNC',date:'2026-09-15',tag:'Version sync, checkpoints and feedback',highlights:[
    'A client open across a deploy now detects the newer build via /api/version and over a protocol-version change, and offers a one-click reload.',
    'Campaign checkpoints persist across sessions and resume the mission at the banked step; the checkpoint clears when the mission is won.',
    'Campaign stars shown on the mission-select screen now match the reward logic (time and score), instead of using a separate time-only rule.',
    'Enemy telegraphs and single-player abilities are captioned and audible, and VIP Escort gets its extraction beacon marker.',
    'Reconnecting a spectator or inviter once again targets their previous room and seat intent.',
  ]},
  {version:'v4.11',codename:'HARDENED',date:'2026-09-15',tag:'Wiring, cache and correctness pass',highlights:[
    'The menu demo now keeps rendering on the room browser, lobby and patch-notes screens instead of freezing, and the rank screen composites its operator preview.',
    'The document is served no-cache so a browser can never keep running a deleted bundle after a deploy; the deployment verifier enforces it.',
    'Objective and reward correctness: team scoreboards for the newer modes, Juggernaut awarded on crown points, and finite soccer goal positions.',
    'Network and lobby robustness: case-insensitive room codes, invite links for spectators, a clear fallback when a room is full or closed, and voice config no longer drops congested sockets.',
    'HUD/UX wiring: practice-vs-bots honors its bot/difficulty pickers, mission select opens from the rank screen, local spectate lists only its real controls, and the radar draws off-screen bearing arrows.',
  ]},
  {version:'v4.10',codename:'CLARITY',date:'2026-09-15',tag:'The demo keeps showing the match',highlights:[
    'The title demo no longer reveals the operator model between scenarios. The view now decides from the showcase setting itself, not from whether a snapshot happens to be ready, so a cycling build can never fall back to the model preview.',
  ]},
  {version:'v4.9',codename:'INVITE',date:'2026-09-15',tag:'Invite links and a clearer title demo',highlights:[
    'Copy an invite link from the lobby or the room browser; opening it auto-connects and drops the player straight into that room, with a fallback to the room browser if the code has closed.',
    'The title-screen vignette was lightened and the broadcast lower-third now sits above it with a darker, higher-contrast card, so the mode, map and score stay readable over the demo.',
  ]},
  {version:'v4.8',codename:'BROADCAST',date:'2026-09-15',tag:'Docs, patch notes and a broadcast title demo',highlights:[
    'A broadcast-style lower-third now overlays the title-screen demo, reporting the live mode, map, score and objective with animated metric blocks and a scrolling ticker.',
    'A Changelog screen joins the menu, showing the running release and recent patch notes with a link to the full history.',
    'The demo background no longer flashes the full-screen operator model between scenarios: the view holds the arena until the next showcase snapshot arrives.',
    'The documentation was rebuilt: a world-class README, a full docs/ tree with architecture, systems and testing references, and a complete CHANGELOG.md.',
  ]},
  {version:'v4.7',codename:'SPECTACLE',date:'2026-09-15',tag:'Menu showcase, vehicles and atmosphere',highlights:[
    'The title reel now shuffles a curated set of mode/map showpieces instead of a fixed order, and rebuilds atomically so it never drops to a static model preview.',
    'Titan, Scout and Transport vehicles are placed on the warzone maps alongside Pumas and Hornets.',
    'Campaign missions author weather (blizzard, ash, storms) with scripted mid-mission changes and timed story transmissions that play as voice-over beats.',
    'A broadcast-style lower-third on the title screen reports the live demo mode, map, score and objective — and a full Changelog screen lands in the menu.',
  ]},
  {version:'v4.6',codename:'TUNED',date:'2026-09-15',tag:'Weapon balance and single-player',highlights:[
    'Distinct roles and retuned time-to-kill for all ten weapons, with DPS/TTK balance metrics.',
    'Out-of-combat health regeneration in campaign and horde (delayed after damage, reset on respawn/resupply/checkpoint).',
    'Speaker/mission-lore data, campaign briefings, procedural weapon rigs, material fidelity presets and richer announcer audio.',
  ]},
  {version:'v4.5',codename:'CONNECTED',date:'2026-09-15',tag:'Netcode, replay and world',highlights:[
    'Snapshot delta compression with bandwidth accounting, deterministic prediction/reconciliation and interpolation, and protocol v2 with a full-snapshot fallback.',
    'Replay kill feed, objective timeline and a seekable summary; cinematic, over-shoulder, free-look and tactical cameras.',
    'Titan/Scout/Transport chassis, levelgen compounds/terraces/towers, biome props and structural map-schema validation.',
    'Server matchmaking with balanced teams, room lifecycle (warmup/ready/map-vote/rematch), leaderboards and anti-cheat bounds.',
  ]},
  {version:'v4.4',codename:'BROADER',date:'2026-09-14',tag:'Objectives, presentation, accessibility',highlights:[
    'New objective modes Holdout (quorum hold) and Uplink (sequential relay); Endless Horde with score banking and escalating bosses.',
    'Economy pickups (weapon upgrade, deployable sentry), in-menu 3D weapon inspect, hit reactions, storm lightning/thunder, wet sheen and wind.',
    'Per-mode music themes and victory/defeat stings; deuteranopia/protanopia/tritanopia palettes, high-contrast UI and full keyboard remapping.',
    'Replay export/import, match summary card, room filters and practice-vs-bots.',
  ]},
  {version:'v4.3',codename:'DEEPER',date:'2026-09-14',tag:'Mutators, maps and career',highlights:[
    'Composable mutators (low gravity, turbo, instagib, one-shot, mirror loadout, big head, no recoil) plus per-mode loadouts and sniper/pistol presets with ammo/megahealth supplies.',
    'Two new maps (Dune Ravine, Ember Caldera) with biome props and deterministic destructible crates and barrels.',
    'Prestige ranks after max level, twelve achievements with unlock toasts, and a Career track panel.',
  ]},
  {version:'v4.2',date:'2026-09-14',tag:'Campaign, weather and meta',highlights:[
    'Five campaign missions with stealth, duel and boss-phase content; horde gains lancer, sentinel and Harbinger enemies plus new wave modifiers.',
    'Deterministic weather and time of day (rain, snow, ash, storm) with pooled visuals and dynamic audio intensity.',
    'Local match history, per-mode leaderboards, weekly challenges, campaign stars/medals, expanded results medals and an Arsenal inspector.',
    'Persisted, selectable quality tiers: Auto, Low, Medium and High.',
  ]},
  {version:'v4.1',date:'2026-09-14',tag:'Modes, single-player and presentation',highlights:[
    'Juggernaut, Team Elimination and VIP Escort modes; multi-phase Warden bosses; sudden-death timers so every mode terminates.',
    'Horde between-wave upgrades and wave modifiers, new enemy roles (mender/sapper/overseer, shield tank, mortar artillery), campaign checkpoint resume and a third mission.',
    'Death variety, ambient FX, audio variants, richer models, impact decals and a quality/LOD controller with a CPU triangle budget.',
    'Daily challenges, per-mode career stats, full loadout presets, a theater library, help legend and a team-grouped spectator board.',
  ]},
  {version:'v4.0',date:'2026-09-14',tag:'The big improvement pass',highlights:[
    'Single-player enemies spawn in authored areas with per-type leashes; both campaign missions run scripted timelines with real win conditions.',
    'Vehicles only spawn in vehicle modes; CTF carriers are slowed and cannot use powers; the KOTH hill rotates and zone ownership grants buffs; Arms Race demotes on death with a catch-up bonus.',
    'A high-poly rolling soccer ball and per-vertex colours in the CPU software renderer.',
    'The results screen shows XP/level/next-unlock, unlocks queue as toasts, and the theater lists jump-to highlights.',
  ]},
  {version:'v3.10',date:'2026-09-14',tag:'Mobile touch controls',highlights:[
    'Floating dual sticks (move left, look right) fix the bug where any left-side touch flung the stick full forward.',
    'Large fire and jump thumb buttons with compact action buttons that no longer overlap, multi-touch safe.',
    'A fullscreen button and a simplified racing layout (brake/reset plus boost/item).',
  ]},
  {version:'v3.0',date:'2026-09-14',tag:'Menu redesign',highlights:[
    'A new design system: one Shell, Panel, Btn, Modal and shared Stats/Tabs/Segmented/Chip/Meter primitives under app/ui/.',
    'Balanced menu layouts with the loadout in the elastic column and a sticky action rail that never falls below the fold.',
    'The previous inline menu markup is replaced, with the legacy boundary recorded in app/legacy/README.md.',
  ]},
  {version:'v2.0',date:'2026-09-13',tag:'Progression, unlocks and gear',highlights:[
    'XP and ranks with a deterministic curve, level rewards and six rank titles from Recruit to Mythic.',
    'Eight gear pieces and three weapon finishes unlock as you level, shown on a new Rank screen.',
    'Equip gear per slot to tune health, armour, speed, damage and spread; server persistence awards results authoritatively.',
  ]},
  {version:'v1.6',date:'2026-09-11',tag:'Theater and a living menu',highlights:[
    'Every finished match is recorded as compact keyframes; replay any recording with scrubbing, speed control and cinematic cameras.',
    'A camera director with seven rigs and auto-cuts to kills, explosions and captures.',
    'The main menu renders a live, auto-directed bot match behind the UI.',
  ]},
  {version:'v0.4',date:'2026-09-07',tag:'Local multiplayer',highlights:[
    'A Node game server authoritative over rooms, with per-actor inputs, sequenced 60 Hz inputs and 20 Hz snapshots.',
    'Client-side prediction and reconciliation for instant movement, aim and fire, plus interpolated remote actors.',
    'Session tokens, seat-holding, reconnection with token reattach, and bot handoff with host migration.',
  ]},
];

// Newest release first, defensively sorted so a bad edit cannot scramble the UI.
export const changelogReleases = () => [...CHANGELOG].sort((a, b) => {
  const parse = v => String(v || '').replace(/^v/, '').split('.').map(n => Number(n) || 0);
  const [a1, a2 = 0, a3 = 0] = parse(a.version), [b1, b2 = 0, b3 = 0] = parse(b.version);
  return b1 - a1 || b2 - a2 || b3 - a3;
});
